# Brain — Architecture

- **`current-state.md`** — snapshot of what's implemented, what's deployed, and what's missing. Update when a module ships or a gap closes.
- **`changelog.md`** — reverse-chronological log of significant changes. Update alongside `current-state.md` whenever architecture, conventions, or module status changes.
